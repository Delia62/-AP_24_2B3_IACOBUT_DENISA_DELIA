package server;

public class Ship {
/*
    private final int size;
    private int hits;
    private final Player player;
    private final String type; // Optional: Ship type (e.g., "battleship")

    public Ship(int size, Player player, String type) {
        this.size = size;
        this.hits = 0;
        this.player = player;
        this.type = type;
    }

    public boolean hit(int row, int col) {
        // Check if the coordinates are within the board boundaries
        if (row < 0 || row >= board.length || col < 0 || col >= board[0].length) {
            return false;
        }

        // Check if the ship occupies the square based on its placement on the board
        int shipRow = getShipRow(); // Replace this with logic to get the ship's starting row based on placement
        int shipCol = getShipCol(); // Replace this with logic to get the ship's starting column based on placement
        int shipOrientation = getShipOrientation(); // Replace this with logic to get the ship's orientation (horizontal/vertical)

        if (shipOrientation == Orientation.HORIZONTAL) {
            // Check for horizontal ship placement
            return (row == shipRow && col >= shipCol && col < shipCol + size);
        } else {
            // Check for vertical ship placement
            return (col == shipCol && row >= shipRow && row < shipRow + size);
        }
    }

    public boolean isSunk() {
        return hits == size;
    }

    public int getSize() {
        return size;
    }

    public Player getPlayer() {
        return player;
    }

    public String getType() {
        return type;
    }*/
}
